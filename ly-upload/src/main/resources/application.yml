server:
  port: 9995

spring:
  application:
    name: upload-service
  servlet:
    multipart:
      max-file-size: 5MB

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:9999/eureka
  instance:
    ip-address: 127.0.0.1
    prefer-ip-address: true